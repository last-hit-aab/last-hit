os:
    - osx
    - windows
language: node_js
node_js:
    - 12
cache:
    yarn: true
env:
    - YARN_GPG=no
branches:
    only:
        - master

script:
    - cd renderer
    - yarn install
    - cd ..
    - yarn install
    - yarn run pack:$TRAVIS_OS_NAME
